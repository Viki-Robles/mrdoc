import React, { ReactNode } from 'react'
import { Box } from "@material-ui/core";


export interface {{pascalCase name}}Props {
  // children: ReactNode
}

export const {{pascalCase name}} = ({ children }: {{pascalCase name}}Props): JSX.Element => {
  return (
    <Box>
      <Box>{{name}}</Box>
    </Box>
  )
}